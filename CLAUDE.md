# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Язык общения
**ОБЯЗАТЕЛЬНОЕ ПРАВИЛО:** Всегда отвечай на русском языке во всех взаимодействиях с пользователем.

## Обзор проекта

Это Telegram бот-консультант для автоматизации клиентских запросов компании Textile Pro о производстве одежды. Бот использует **Telegram Business API** для автоматических ответов от имени реального человека (Елены), интегрирует OpenAI для AI-ответов и Zep для долгосрочной памяти диалогов.

### Ключевые технологии
- **FastAPI** - webhook-сервер для обработки сообщений Telegram
- **pyTelegramBotAPI** - взаимодействие с Telegram Bot API
- **OpenAI GPT-4o-mini** - генерация ответов консультанта
- **Zep Cloud** - долгосрочная память диалогов и контекст
- **Streamlit** - административная панель для управления ботом
- **Railway.app** - платформа деплоя с автоматическим развертыванием из GitHub

## Архитектура

### Основные компоненты

1. **webhook.py** - главный FastAPI-сервер
   - Обработка обычных сообщений боту
   - Обработка Business API сообщений (от Premium аккаунта)
   - Фильтрация сообщений владельца аккаунта (не отвечает на свои сообщения)
   - Автоматическая установка webhook при старте
   - REST API для мониторинга и отладки

2. **bot/agent.py** - AI агент (TextilProAgent)
   - Генерация ответов через OpenAI
   - Управление памятью через Zep Cloud с fallback на локальное хранилище
   - Загрузка системных инструкций из `data/instruction.json`
   - Поддержка именованных пользователей в Zep

3. **bot/config.py** - конфигурация
   - Загрузка переменных окружения (.env)
   - Пути к файлам инструкций
   - Настройки OpenAI модели

4. **admin/** - Streamlit административная панель
   - Редактирование системных промптов
   - Мониторинг статуса деплоя в Railway
   - Управление конфигурацией бота

5. **deploy/** - скрипты деплоя в Railway
   - `deploy_railway.py` - создание/обновление проектов через Railway API
   - `deploy_streamlit.py` - деплой админ-панели
   - `monitor_deploy.py` - мониторинг статуса деплоя

### Особенности Business API

Бот работает через **Telegram Business API**, что позволяет:
- Отвечать от имени реального пользователя (Елены из Textile Pro)
- Клиент видит ответ как будто написал человек, не бот
- Фильтрация сообщений владельца: бот **НЕ отвечает** на сообщения самого владельца аккаунта
- Обработка `business_connection` событий для отслеживания владельцев

**ВАЖНО:** Отправка через Business API требует `business_connection_id` и использует прямой HTTP запрос к Telegram API (pyTelegramBotAPI не поддерживает этот параметр).

### Система памяти

- **Zep Cloud** (приоритет) - облачное хранилище контекста диалогов
- **Local fallback** - локальное хранилище в памяти если Zep недоступен
- Session ID: `user_{user_id}` для обычных сообщений, `business_{user_id}` для Business API
- Хранение последних 6 обменов для контекста
- Именованные роли: используется имя пользователя вместо generic "user"

### Фильтрация старых сообщений

Бот игнорирует сообщения старше 5 минут при запуске (предотвращает лавину старых уведомлений при деплое).

## Команды разработки

### Локальная разработка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск webhook сервера (требует .env с токенами)
python webhook.py

# Запуск Streamlit админ панели
python run_admin.py
# или
streamlit run admin/streamlit_admin.py

# Проверка Business API
python3 check_business_api.py

# Тест отправки через Business API
python3 test_business_api.py

# Тест Zep Memory
python3 test_zep.py
```

### Тестирование

```bash
# Проверка последних updates для отладки
curl https://bot-production-472c.up.railway.app/debug/last-updates

# Проверка статуса Zep
curl https://bot-production-472c.up.railway.app/debug/zep-status

# Проверка памяти конкретной сессии
curl https://bot-production-472c.up.railway.app/debug/memory/user_123456

# Проверка владельцев Business Connections
curl https://bot-production-472c.up.railway.app/debug/business-owners

# Информация о webhook
curl https://bot-production-472c.up.railway.app/webhook/info

# Установка webhook (автоматически при старте)
curl https://bot-production-472c.up.railway.app/webhook/set
```

### Деплой в Railway

**КРИТИЧЕСКИ ВАЖНО:** Railway автоматически деплоит при push в GitHub main.

```bash
# Проверить изменения
git status
git diff

# Зафиксировать изменения
git add .
git commit -m "Описание изменений

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>"

# Отправить на GitHub (автоматический деплой в Railway)
git push origin main
```

**Правило автоматического обновления GitHub:**
- ВСЕГДА обновляй GitHub после изменений в `bot/`, `webhook.py`, `requirements.txt`, `deploy/`
- Railway автоматически деплоит изменения с GitHub
- Это обеспечивает синхронизацию локального кода с продакшеном

### Переменные окружения (Railway)

Обязательные:
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `OPENAI_API_KEY` - ключ OpenAI API
- `ZEP_API_KEY` - ключ Zep Cloud API
- `WEBHOOK_SECRET_TOKEN` - секрет для защиты webhook
- `BOT_USERNAME` - имя бота (например, @artyom_integrator_bot)

Опциональные:
- `WEBHOOK_URL` - URL webhook (по умолчанию Railway автоматически)
- `PORT` - порт (по умолчанию 8000)

### Структура файлов

```
bot/
├── agent.py          # AI агент с OpenAI + Zep
├── config.py         # Конфигурация и переменные окружения
└── __init__.py

admin/
├── streamlit_admin.py      # Главный UI админ панели
├── auth.py                 # Аутентификация (password)
├── config.py               # Конфигурация админки
└── deploy_integration.py   # Интеграция с Railway API

deploy/
├── deploy_railway.py       # Деплой через Railway API
├── deploy_streamlit.py     # Деплой Streamlit админки
├── monitor_deploy.py       # Мониторинг деплоя
├── quick_monitor.py        # Быстрый мониторинг
├── deploy_to_existing.py   # Обновление существующего проекта
└── delete_project.py       # Удаление проекта

data/
└── instruction.json        # Системный промпт для бота

scripts/
├── clear_pending_updates.py    # Очистка старых updates
└── security_monitor.py         # Мониторинг безопасности

webhook.py              # Главный FastAPI сервер
requirements.txt        # Зависимости Python
Dockerfile.complete     # Docker образ для Railway
```

## Работа с системным промптом

Системный промпт хранится в `data/instruction.json`:

```json
{
  "system_instruction": "Подробная инструкция для AI...",
  "welcome_message": "Приветственное сообщение",
  "last_updated": "2025-07-29T08:12:48.040623"
}
```

**Важные правила промпта:**
- Бот играет роль Елены, консультанта Textile Pro
- Обязательное форматирование: абзацы с двойными переносами строк
- НЕ приветствовать в каждом сообщении, только при первом контакте
- Отвечать только на основе информации из базы данных
- НЕ давать точные цены, только "от (сумма)"
- НЕ отвечать на односложные благодарности ("Спасибо", "Хорошо")

Перезагрузка промпта:
```bash
curl -X POST https://bot-production-472c.up.railway.app/admin/reload-prompt
```

## Мониторинг и отладка

### Логи

Railway автоматически собирает логи:
```bash
railway logs
```

Локальные логи хранятся в `logs/bot.log` (ротация при 10MB).

### REST API endpoints

- `GET /` - health check и статус бота
- `GET /webhook/info` - информация о webhook
- `POST /webhook/set` - установка webhook
- `GET /debug/last-updates` - последние 10 updates
- `GET /debug/zep-status` - статус Zep Memory
- `GET /debug/memory/{session_id}` - память конкретной сессии
- `GET /debug/business-owners` - список владельцев Business Connections
- `GET /debug/prompt` - текущий системный промпт
- `POST /admin/reload-prompt` - перезагрузка промпта

## Типичные задачи

### Изменение логики бота

1. Редактируй `bot/agent.py` (AI логика) или `webhook.py` (обработка событий)
2. Тестируй локально с `.env`
3. Commit и push в GitHub
4. Railway автоматически деплоит (2-3 минуты)
5. Проверь логи: `railway logs`

### Обновление системного промпта

1. Редактируй через Streamlit админку ИЛИ напрямую `data/instruction.json`
2. Если редактировал вручную: `curl -X POST .../admin/reload-prompt`
3. Проверь: `curl .../debug/prompt`
4. Commit и push для синхронизации с GitHub

### Добавление новой функциональности

1. Обнови `bot/agent.py` для AI логики
2. Обнови `webhook.py` для обработки новых типов сообщений
3. Обнови `requirements.txt` при новых зависимостях
4. Тестируй локально
5. Push в GitHub для автоматического деплоя

### Отладка проблем с Business API

1. Проверь `/debug/last-updates` - видны ли `business_message` события
2. Проверь `/debug/business-owners` - правильно ли сохранен owner_id
3. Проверь логи на фильтрацию сообщений владельца
4. Используй `test_business_api.py` для тестовой отправки

## Важные нюансы

- **Webhook vs Polling:** Бот работает ТОЛЬКО в режиме webhook, polling отключен
- **Business API отправка:** Используй `send_business_message()` с `business_connection_id`
- **Фильтр владельца:** Сообщения где `user_id == owner_user_id` игнорируются
- **Старые сообщения:** Сообщения старше 5 минут игнорируются при старте
- **Fallback логика:** Если OpenAI недоступен - простые шаблонные ответы
- **Zep fallback:** Если Zep недоступен - локальное хранилище в памяти
- **Вложения без текста:** Бот просит пользователя прокомментировать ("Фото увидела. Прокомментируйте...")

## Связь с админ панелью

Streamlit админка работает на отдельном Railway сервисе:
- SSH туннель для доступа: `ssh -L 8502:localhost:8501 coder@104.248.39.106`
- Браузер: `http://localhost:8502`
- Пароль: `password` (определен в `admin/auth.py`)
- Функции: редактирование промпта, мониторинг Railway, управление деплоем

## Безопасность

- Webhook защищен `WEBHOOK_SECRET_TOKEN` (проверка в заголовке `X-Telegram-Bot-Api-Secret-Token`)
- Админ панель защищена паролем
- API ключи в переменных окружения (не в коде)
- `.env` в `.gitignore`

## Контакты и ссылки

- **Webhook URL:** https://bot-production-472c.up.railway.app/webhook
- **Статус:** https://bot-production-472c.up.railway.app/
- **Railway Dashboard:** https://railway.app/dashboard
- **GitHub:** https://github.com/Timosan61/Textill_PRO_BOT
